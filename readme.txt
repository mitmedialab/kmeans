K-means tool 

- What is clustering?

- K-means clustering introduction (define, diagrams, etc.)

- Let's make some K-means clusters from custom points(live clustering tool)
-- Effect of outliers
-- Effect of changing the K value 
-- Effect of sizes of the clusters

- Let's create a dataset and plot it. (example dataset). Form clusters.
-- Custom axes 
-- Naming the clusters 


Home
Clustering
K-Means
Tool
Datasets